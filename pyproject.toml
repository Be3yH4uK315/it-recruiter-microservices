[tool.black]
line-length = 100
target-version = ["py312"]
skip-string-normalization = false
exclude = '''
/(
    \.git
  | \.venv
  | __pycache__
  | migrations
  | build
  | dist
)/
'''

[tool.ruff]
line-length = 100
target-version = "py312"

exclude = [
    ".git",
    ".venv",
    "__pycache__",
    "migrations",
    "build",
    "dist"
]

[tool.ruff.lint]
select = [
    "E",
    "F",
    "I",
    "B"
]

ignore = [
    "E501",
    "B008"
]

[tool.ruff.lint.isort]
known-first-party = ["app", "loadtests"]

[tool.isort]
profile = "black"
line_length = 100
known_first_party = ["app", "loadtests"]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"
line-ending = "auto"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = [
    "tests",
    "services"
]
python_files = [
    "test_*.py",
]
