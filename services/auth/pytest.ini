[pytest]
pythonpath = .
testpaths = tests
asyncio_mode = auto
addopts = -ra -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    integration: marks tests requiring integration environment
